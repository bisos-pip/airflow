#+title: Development Status & Continuation Handoff
#+TAGS: me ai aiAndMe

# Provenance (installed once by aiActivity.cs --- not regenerated):
#   Date:      2026-09-01T12:55:42-07:00
#   User:      bystar
#   Templates: /bisos/apps/defaults/ai-templates
#   Command:   /bisos/venv/py3/devbin/aiActivity.cs -i initiate --activity=bisos-pip

####+BEGIN: b:ai:file/particulars
* AI Working Context
  Working Directory: /bisos/git/auth/bxRepos/bisos-pip/airflow
  File: /bisos/git/auth/bxRepos/bisos-pip/airflow/AI-DevStatus.org
  Activity: bisos-pip
  Companion Docs: AI-WORKFLOW.org, AI-Activity.org, AI-DevStatus.org, README.org
####+END:

This file is a working-state handoff so development can continue on another
machine (which has this repo but NOT the developer's =~=). It captures the
architecture, what works, what is untested and the design decisions made so far.

This file is maintained by Claude. Updates are typically triggered by the
=/bx-update-status= slash command, after major interactions, or before a release.

Note to a fresh Claude: this file is intentionally sparse at project start.
Fill in the sections below when asked to update, following the structure provided.

* Current State

  =AI-Usage.org= (in this repo's root, alongside this file) documents what
  =bisos.airflow= actually does and its current gaps --- package scope, the
  four systemd unit templates, the =api-server=/=apache-airflow>=2.9.0=
  version-floor mismatch, "executor/backend and cross-venv DAG bridging are
  not this package's concern", and package maturity (no Python library/CSU
  surface yet, most Blee-panel content beyond =serverSetup= is aspirational).
  It is deliberately *not* imported by this repo's own =CLAUDE.md= --- it's
  written to be =@=-imported by downstream projects that deploy Airflow the
  BISOS way (first consumer: =site-status-scripts='s
  =airflow/AI-Activity.org=), so a fresh Claude session in *this* repo won't
  see it unless it opens the file directly. Tracked in git, same as the rest
  of this repo. Content last verified against source on 2026-09-10.

  [Further current-state notes to be filled in by Claude after first
  substantive interaction.]

* Architecture
  [Key components, file structure, entry points]

* What Works
  [Verified functionality]

* What Is Untested
  [Known gaps]

* Key Design Decisions
  [Non-obvious choices and their rationale]

# Local Variables:
# eval: (setq-local toc-org-max-depth 4)
# End:
